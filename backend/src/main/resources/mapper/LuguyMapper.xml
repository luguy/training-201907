<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.com.augmentum.backend.dao.LuguyMapper" >
  <resultMap id="BaseResultMap" type="cn.com.augmentum.backend.pojo.Luguy" >
    <result column="name" property="name" jdbcType="VARCHAR" />
    <result column="age" property="age" jdbcType="REAL" />
  </resultMap>
  <sql id="Base_Column_List">
    name, age
  </sql>

  <insert id="insert" parameterType="cn.com.augmentum.backend.pojo.Luguy" >
    insert into luguy (name, age)
    values (#{name,jdbcType=VARCHAR}, #{age,jdbcType=REAL})
  </insert>
  <insert id="insertSelective" parameterType="cn.com.augmentum.backend.pojo.Luguy" >
    insert into luguy
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="name != null" >
        name,
      </if>
      <if test="age != null" >
        age,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="name != null" >
        #{name,jdbcType=VARCHAR},
      </if>
      <if test="age != null" >
        #{age,jdbcType=REAL},
      </if>
    </trim>
  </insert>

  <select id="selectAll" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from luguy
  </select>

</mapper>